/**
 * The one-time, non-consumable purchase that removes both the banner and the
 * interstitial. This exact ID must exist as an in-app product in both App
 * Store Connect and the Google Play Console before a purchase can succeed -
 * nothing here creates it automatically.
 */
export const REMOVE_ADS_SKU = 'remove_ads';
