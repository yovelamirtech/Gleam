# Expo HAS CHANGED

This app targets Expo ~57 (see `package.json`). A model's training data can predate
API changes made since then, so read the exact versioned docs at
https://docs.expo.dev/versions/v57.0.0/ before writing any Expo/React Native code
here, rather than relying on memory.
