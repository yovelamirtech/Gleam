# Gleam — תוכנית בנייה מלאה

מסמך זה מיועד להדבקה ל-Claude Code כנקודת פתיחה לפרויקט. הוא כולל את כל ההחלטות שנסגרו, וכן נקודות שסומנו במפורש כ"לשאול את המשתמש כשמגיעים אליהן".

## סקירה כללית

Gleam הוא משחק דיאמונד פיינטינג למובייל (Expo / React Native), באנגלית בלבד. המשתמש ממלא רשתות תאים ממוספרות באבני "יהלום" צבעוניות, בדיוק כמו ציור יהלומים פיזי. כל level הוא תמונה ענקית שמחולקת לרשת של בורדים, וכל בורד נפתח כשמשלימים שכן שלו — אותה מכניקת "שכנים נפתחים" חלה גם על מסך ה-levels עצמו (מבנה פרקטלי).

## מבנה הפרויקט (Expo + React Native)

- Stack: Expo, React Native, **react-native-skia** לרינדור הגריד (לא react-native-svg — יותר מדי אלמנטים בקנה מידה הזה)
- react-native-gesture-handler + reanimated לגרירה, סיבוב וזום/פאן
- AsyncStorage לשמירת התקדמות מקומית (פר-מכשיר, בלי שרת)
- כל הטקסטים והתוכן במשחק באנגלית בלבד
- כיוון מסך: תומך גם בפורטרט וגם ב-landscape — כל המסכים (levels, בורד, הגדרות) צריכים לתמוך בשני הכיוונים

## מבנה תמונה ולוח (Board)

- כל level = תמונה אחת, מחולקת ל-48 בורדים בפריסה של 8×6 (רוחב × גובה)
- כל בורד = 40×40 תאים
- רזולוציה כוללת ל-level: 320×240 תאים
- פלטת צבעים אחידה לכל התמונה השלמה — ה-quantization מתבצע על התמונה כולה מראש, לא בורד-בורד בנפרד, כדי שהצבעים יתחברו בגבולות בין בורדים
- טעינה עצלה (lazy loading): טוענים רק את הבורד הנוכחי + שכנים פוטנציאליים, לא את כל הבורדים בבת אחת

### פתיחת בורדים
- מתחילים מבורד מרכזי אחד
- השלמת בורד פותחת אוטומטית את הבורדים הצמודים אליו (מעלה/מטה/ימין/שמאל)
- State פשוט: `boards[boardId].status = locked | unlocked | completed`

## מסך ה-Levels ("קיר תמונות")

- תצוגת רשת גדולה של כל ה-levels, ויזואלית מזכירה את מבנה הבורדים בתוך level
- Level 1 פתוח בהתחלה
- השלמת level (כלומר השלמת כל 48 הבורדים שלו) פותחת את ה-levels השכנים לו באותה רשת
- Level "פתוח" = ניתן ללחוץ עליו ולהיכנס למסך הבורדים שלו; level "נעול" = מוצג עמום/עם מנעול

## מכניקת ההדבקה (HUD ואבנים)

- כל תא בגריד מציג **מספר בלבד**, לא צבע
- HUD (בתחתית או בצד) עם **5 משבצות אבן**
- המשתמש בוחר צבע/מספר → 5 המשבצות מתמלאות באבנים מאותו צבע, נלקחות ממלאי הבורד
- **חשוב — כלכלת האבנים:** בכל בורד, כמות האבנים הזמינה לכל צבע שווה **בדיוק** לכמות התאים שדורשים את אותו צבע באותו בורד. אף פעם לא ניתן להיתקע באמצע בלי מספיק אבנים להשלים — המלאי מובטח להספיק בדיוק
- גוררים 1-5 אבנים מה-HUD כרצועה (strip)
- לחיצה על הרצועה מסובבת אותה בין אנכי לאופקי
- הרצועה "נעצרת" ומונחת במקום שבו המשתמש משחרר את האצבע
- התחלת בחירה חדשה (נגיעה חוזרת באבנים ב-HUD), או שחרור מחוץ לגריד, מחזירה אוטומטית את הרצועה הנוכחית למגש — **בלי** כפתור מחיקה/החזרה נפרד
- תוך כדי משחק בבורד בודד (40×40 תאים) נדרש **זום/פאן** — הבורד לא נראה כולו בבת אחת על המסך, המשתמש מתקרב/מתרחק ונע בתוכו כדי להדביק בדיוק
- **כפתור יציאה** בפינת מסך הבורד — לוחצים עליו כדי לחזור למסך ה-levels באמצע משחק (בנוסף למקש חזור של המערכת)

## עיצוב ויזואלי — פאוקס-3D ליהלומים

- כל יהלום מצויר עם גרדיאנט (בהיר בפינה עליונה-שמאלית, כהה בנגדית) + highlight קטן + צל עדין מתחת — כדי לתת תחושת בליטה בלי מנוע 3D אמיתי
- מקור אור **סטטי** (לא זז עם gyroscope/הטיית מכשיר) — יעיל יותר לביצועים וסוללה
- **אין** נצנוץ אמביינטי אקראי על יהלומים (הוסר מהתוכנית)
- הברק/הליטוש מגיע מהעיצוב הפאוקס-3D עצמו (גרדיאנט + highlight), לא מאנימציה נפרדת

## עיצוב UI כללי ואווירה

- פלטת צבעים **בהירה** לכל ממשק המשחק (כפתורים, רקעים, תפריטים) — לא כהה/יוקרתי
- מסך ה-levels ("קיר התמונות") כולל ציורי רקע **עדינים ומעטים** סביב התמונות — קווי ספירלה או צורות דומות, מינימליסטי ולא עמוס
- App Icon — ~~אין צורך בקובץ מהמשתמש; Claude Code ייצר/יעצב סמל אפליקציה בעצמו כחלק מהפרויקט~~ בוצע:
  אבן עגולה מלמעלה עם 12 פאונטות טרפזיות סביב "table" מרכזי, כמו אבני ציור-יהלומים אמיתיות (ר' `tools/app-icon/`). עבר שני עיצובים קודמים (אשכול ריבועים מעוגלים, ואז יהלום מזווית צד) לפי פידבק — זה השלישי והמאושר. **ראו HANDOFF.md** — יש כוונה לעדכן גם את איך שהאבנים במשחק עצמו (`src/ui/drawStone.ts`) נראות, כדי שיתאימו לסגנון הזה.

## מסך סיום Level (חגיגת השלמה)

ברגע שמשלימים level שלם (כל 48 הבורדים), רצף אנימציה:
1. **זום-אאוט** על כל התמונה השלמה, כדי שהיא נראית כולה על המסך
2. **ניצנוץ חגיגי** על פני כל התמונה (אפקט חד-פעמי, לא הנצנוץ האמביינטי שהוסר מהמשחק הרגיל)
3. **היעלמות מהירה של כל האבנים בסדר הפוך** לסדר ההדבקה שלהן (האבן שהודבקה אחרונה נעלמת ראשונה) — מלווה בזום-אין קל
4. **הופעה מחדש מהירה של כל האבנים לפי סדר ההדבקה המקורי** (מהראשונה לאחרונה) — כך שנראה כאילו התמונה "מציירת את עצמה" מחדש, בדיוק כפי שהמשתמש פתר אותה

**דרישה טכנית:** יש לשמור **סדר/timestamp של הדבקת כל אבן** (לא רק את המיקום הסופי שלה) בכל בורד, כדי שהאנימציה הזו תוכל לשחזר את סדר הפתרון האמיתי של המשתמש.

**עדכון:** בוצע — `LevelCompleteScreen` + `src/game/levelReplay.ts` (ר' README, "What is built"). לא נבדק במכשיר אמיתי לתחושה/קצב פריימים; רק לוגיקת הסדר מכוסה בטסטים אוטומטיים. אין עדיין זום-אין קל בשלב ההיעלמות (סעיף 3) — רק ההיעלמות/הופעה מחדש עצמן.

## מסכי פתיחה וניווט

1. **מסך פתיחה**: לוגו הסטודיו (המשתמש יעלה קובץ קיים) → מעבר למסך "Tap to Start" עם סמל המשחק

   **עדכון:** בוצע — `SplashScreen` מציג את `assets/studio_logo/wordmark.png` (מרונדר מ-`wordmark.svg` שהמשתמש סיפק) לזמן קצר ועובר אוטומטית ל-`TapToStartScreen`, שמציג את `assets/icon.png` (סמל האבן הקיים) עם "Tap to Start"; לחיצה עליו נכנסת למסך ה-Levels. לא נבדק על מכשיר אמיתי לתחושה/עיתוי המעבר.
2. **מסך Levels** (קיר התמונות, ר' מעלה)
3. **מסך בורד** (המשחק עצמו)
4. **מסך הגדרות** — נגיש דרך סמל הגדרות שמופיע בכל מסך

## מסך הגדרות

- צלילים/מוסיקה (on/off)
- About / קרדיטים
- איפוס התקדמות (מתחילים מחדש)

**עדכון:** אין cross-promotion למשחקים אחרים של הסטודיו במסך ההגדרות — הוחלט במפורש לא לכלול זאת. הריפואים של "Bullseye Words" ו-"Letter Wheel" (https://github.com/yovelamirtech/letter-wheel, https://github.com/yovelamirtech/bullseye-words) שימשו רק כהשראה לסגנון הוויזואלי של מסך ההגדרות עצמו (מבנה sections/rows, Toggle מותאם אישית, טוקני צבע), לא לקישור בפועל בתוך האפליקציה.

## Onboarding (הסבר ראשוני)

- בכניסה הראשונה למסך בורד, overlay שמדגיש/מצביע על אלמנטים מרכזיים (איך לבחור צבע, איך לגרור אבנים) עם prompts של "הקש כאן"
- מוצג פעם אחת בלבד (נשמר ב-AsyncStorage שהוצג), עם אפשרות Skip

**עדכון:** בוצע — `OnboardingOverlay` (`src/components/OnboardingOverlay.tsx`), מחובר ב-`BoardScreen`. שני צעדים: מצביע על שורת הצבעים (`ColorPicker`) ואז על המגש (`HudTray`), כל אחד עם כרטיס טקסט מעליו ו"Skip"/"Next"/"Got it". רק שורה עליונה+תחתונה מוצללות (spotlight על הרצועה המלאה, בלי חור מדויק — שתי השורות ממילא ברוחב מלא). נשמר ב-`gleam:onboarding:v1` דרך `src/storage/onboarding.ts`, פעם אחת גלובלית (לא פר-בורד). לא נבדק על מכשיר אמיתי — ר' HANDOFF.md לרשימת הבדיקות.

## פרסומות ומונטיזציה

- באנר פרסומת קבוע (מוצג לאורך המשחק)
- פרסומת מלאה (interstitial) בין levels — מוצגת כשמשלימים level ועוברים למסך הבא
- **רכישה חד-פעמית להסרת פרסומות** (IAP) — מסירה גם את הבאנר וגם את ה-interstitial; מומלץ להשתמש ב-expo-in-app-purchases או RevenueCat
- רשת הפרסומות: **AdMob**
- **אין** התראות push בפרויקט

**עדכון:** בוצע — `react-native-google-mobile-ads` (באנר + interstitial) ו-`expo-iap` (רכישה חד-פעמית, לא `expo-in-app-purchases` שכבר deprecated). שני אלה מודולי native, אבל עטופים כך ש-`expo start` הרגיל (Expo Go) **ממשיך לעבוד** לכל שאר האפליקציה — רק הפרסומות עצמן לא נטענות שם (ר' HANDOFF.md, "Ads/IAP and plain Expo Go"); כדי לראות פרסומות/רכישה אמיתיות בפועל עדיין צריך Expo Dev Client דרך EAS Build (`eas.json` נוסף עם פרופיל `development`, ור' HANDOFF.md לצעדים שנשארו — bundle identifier / חשבון AdMob אמיתי / IAP product). הבאנר מוצג בתחתית מסכי Levels ו-Boards (לא במסך המשחק עצמו, כדי לא להתחרות עם הקנבס); ה-interstitial מוצג פעם אחת בכל כניסה ל-`LevelCompleteScreen` (סיום level שלם, לא כל board). "הסר פרסומות" ו"שחזר רכישות" ב-`SettingsScreen`.

## צלילים ומוזיקה

- צליל קל בכל הדבקת אבן
- צליל בהשלמת שורה
- פנפייר בהשלמת בורד שלם
- מוזיקת רקע שקטה ברקע לאורך כל המשחק (ניתנת לכיבוי בהגדרות)
- **Haptic feedback** (רטט קל) בכל הדבקת אבן

**עדכון:** בוצע — `expo-audio` ל-4 הצלילים (`src/audio/useGameSounds.ts` + `BackgroundMusic.tsx`), `expo-haptics` להדבקה. `SettingsProvider` (`src/hooks/useSettings.tsx`) הוא context משותף שנטען פעם אחת בשורש האפליקציה, כדי שכיבוי/הדלקה במסך ההגדרות ישפיע מיד על המוזיקה והצלילים בלי לחכות לרענון מסך. **הקבצים עצמם (`assets/sounds/*.wav`) הם placeholder מסונתז בקוד** (`tools/sound-gen/`), לא סאונד דזיין אמיתי — טיק, שני צלילי-שורה, פנפייר, ופאד רקע שקט בלופ — עד שיהיה סאונד אמיתי להחליף איתו (ר' HANDOFF.md). "השלמת שורה" מוגדרת כשורת-תא (40 תאים) בבורד אחד מתמלאת לגמרי — לא היה concept כזה קודם במודל המשחק, נוסף ל-`BoardSession.place()` (`completedRows` ב-`PlaceResult`). לא נבדק על מכשיר אמיתי.

## סקריפט הכנת תמונות (Prep Script)

- סקריפט (Node או Python) שמקבל תמונה רגילה מהמשתמש והופך אותה ל-level שלם
- שלבים: הקטנה לרזולוציית היעד (320×240), quantization לפלטת צבעים מוגבלת (כ-20-30 צבעים) על התמונה השלמה, חלוקה ל-48 קבצי JSON (אחד לכל בורד, 40×40 תאים כל אחד) עם מיקום וצבע/מספר לכל תא
- יש לתמוך בהרצה על מספר תמונות בבת אחת (batch), כדי שהמשתמש יוכל להזין כמה levels יחד

## כלי פיתוח (Dev Tools)

כל הכלים האלה צריכים להיות **מסומנים בבירור ככלי פיתוח** (למשל תחת flag אחד מרכזי כמו `__DEV_TOOLS__` או תפריט dev נפרד), כך שיהיה קל להסיר/לכבות אותם לפני שחרור:

- פותח שלבים אוטומטי (משחרר את כל ה-levels וכל הבורדים)
- קפיצה מהירה לבורד ספציפי (בחירת מספר level/בורד)
- ניקוי התקדמות שמורה (לבדיקה מחדש מאפס)
- מצב FPS/ביצועים על המסך
- השלמת בורד מידית (ממלא את הבורד הנוכחי באבנים בלחיצה אחת)
- הצגת הצבע/מספר הנכון על כל תא (מצב "פתרון" לבדיקת התאמת תמונה)
- כפתור דילוג מהיר למסך התצוגה הפאוקס-3D בלי מגבלות משחק (לבדיקת עיצוב חופשית)

**עדכון:** בוצע — הכל מאחורי `DEV_TOOLS_ENABLED` (`src/constants/devTools.ts`), שהוא פשוט `__DEV__` של React Native: `false` אוטומטית בכל build של release, בלי צורך להסיר קוד ידנית. תפריט מרכזי `DevToolsScreen` (נגיש משורה ב-`SettingsScreen`, מוצגת רק ב-dev) מכיל: פתיחת כל ה-levels/בורדים (`unlockAll` ב-`src/storage/progress.ts`), קפיצה ישירה ל-level/בורד (steppers), איפוס התקדמות, ומתג FPS overlay (`src/components/FpsOverlay.tsx`, צף מעל כל מסך). "השלמת בורד מידית" (`BoardSession.completeInstantly()`) ו"הצגת פתרון" (prop חדש ב-`BoardCanvas`) יושבים על מסך הבורד עצמו במקום בתפריט המרכזי, כי הם צריכים session חי. גלריית הסגנון הפאוקס-3D החופשית — `DevStoneGalleryScreen`, כל צבעי הפלטה בכמה גדלים, בלי מגבלות משחק. לא נבדק על מכשיר אמיתי.

## סיכום נקודות פתוחות שדורשות קלט מהמשתמש בהמשך

1. ~~קישור לריפו "Bullseye Words" / "Letter Wheel" — לסגנון מסך ההגדרות וסמל ההגדרות~~ סופק:
   https://github.com/yovelamirtech/letter-wheel, https://github.com/yovelamirtech/bullseye-words
2. ~~קישורים למשחקים אחרים של הסטודיו — לקישור ה-cross-promotion בהגדרות~~ הוסר מהתוכנית, לא נדרש
3. ~~קובץ לוגו הסטודיו (המשתמש יעלה)~~ סופק: `assets/studio_logo/wordmark.svg` (ומשמש את מסך הפתיחה)
4. תמונות המקור ל-levels הראשונים (המשתמש יספק)
